"""
Pytest configuration. Its mere presence at the project root puts the root on
sys.path, so tests can `from tools import ...` and `from agent import ...`
regardless of the directory pytest is invoked from.
"""
